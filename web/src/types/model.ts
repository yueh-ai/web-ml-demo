// Re-export model-related types from worker-messages for convenience
export type { ModelMetadata, FeatureData } from './worker-messages';

// Additional model-related types can be added here as needed